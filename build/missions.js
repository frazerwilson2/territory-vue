var missions = new Vue({
  el: '#missions',
  data: {
  },
  methods: {
  	acessPlayerMission: function(id){
  		var response;
  		switch(id){
  			case 1:
          // Do whatever checks if value met true
          break;
        case 2:
          // Do whatever checks if value met true
          break;
        default: return false;
  	}
  }
});